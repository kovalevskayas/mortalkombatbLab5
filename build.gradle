/*
 * This file was generated by the Gradle 'init' task.
 *
 * This is a general purpose Gradle build.
 * Learn more about Gradle by exploring our samples at https://docs.gradle.org/8.0.2/samples
 */
plugins {
    id 'java'
    id 'application'
}
repositories {
    mavenCentral()
    // maven { url 'https://jitpack.io' }
}

dependencies {
    // implementation group: 'com.github.javafaker', name: 'javafaker', version: '1.0.2'
    implementation group: 'org.apache.poi', name: 'poi', version: '5.2.3'
    implementation group: 'org.apache.poi', name: 'poi-ooxml', version: '5.2.3'
    // implementation 'com.github.moove-it:fakeit:v0.5'
}

jar {
    manifest {
        attributes 'Main-Class': 'edu.mephi.MortalKombatBVersion'
    }
  from {
    configurations.runtimeClasspath.collect { it.isDirectory() ? it : zipTree(it) }
  } {
    duplicatesStrategy = DuplicatesStrategy.EXCLUDE
  }
}

ext {
    javaMainClass = "edu.mephi.MortalKombatBVersion"
}

processResources {
    duplicatesStrategy = DuplicatesStrategy.EXCLUDE

  }

application {
    mainClassName = javaMainClass
}

sourceSets {
    main {
        resources {
            srcDir "src/main/resources"

        }
    }
}
